{
  "name": "senkublog",
  "version": "1.0.0",
  "description": "Senkublog",
  "main": "upload.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "hugo -D",
    "upload": "node upload.js",
    "deploy": "npm run build && npm run upload"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "node-scp": "0.0.11",
    "ora": "^5.3.0"
  }
}
